import { Meta } from "@storybook/react";
import Error from "src/app/[locale]/search/error";

const meta: Meta<typeof Error> = {
  title: "Components/Search/Error",
  component: Error,
};
export default meta;

export const Default = {};
