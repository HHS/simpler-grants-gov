# Overview
This folder contains a configuration for elasticmq and is used
when we spin up the service locally for mocking out SQS.

The custom.conf file is passed into the elasticmq docker
during startup in the docker-compose file.

See [the docs](https://github.com/softwaremill/elasticmq) for more detail.