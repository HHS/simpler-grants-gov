# Generated by terraform's archive_file data sources at apply time.
scanner.zip
freshclam.zip

# build-layer.sh scratch dir.
build/

# Note: layer.zip is intentionally NOT ignored. A stub is committed so
# terraform validate works on a fresh checkout; build-layer.sh overwrites
# it locally with the real ClamAV binaries before deploy.
